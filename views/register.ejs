<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>registro</title>
</head>
<body>
    <h1>Registro</h1>
    <!--Este locals.error sirve si mandé los datos con .array(), para .mapped(), se utiliza otro parámetro-->
    <% if(locals.errors) { 
        errors.forEach(err => { %>
        <p style="color: red"><%= err.msg %> </p>
    <% }); }%> 
    <form action="/register" method="POST">
        <label for="">Nombre</label>
        <br><br>
        <input type="text" name="name">
        <br>
        <label for="">Correo</label>
        <br><br>
        <input type="email" name="email">
        <br>
        <label for="">Contraseña</label>
        <br><br>
        <input type="password" name="password">
        <br><br>
        <select name="profile" id="">
            <% let profile = ['Administrador', 'Cliente'] %>
            <% for(let i = 0; i < profile.length; i++) {%>  
            <option value="<%= profile[i] %>"><%= profile[i] %></option>
            <% } %> 
        </select>
        <br><br>
        <input type="submit" value="Registrarme">
    </form>
</body>
</html>